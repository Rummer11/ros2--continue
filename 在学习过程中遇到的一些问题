# 一.节点无法运行
1.新节点可能没在setup.py文件下的entry_point下面注册
2.注册的时候节点名称和等号之间一定要有一个空格。
3.init.py文件丢失：不要随便修改inti.py文件
4.每次运行节点的时候要加载ros2和本地空间的环境变量（source ～/....）
5.节点文件缺少了py/cpp后缀导致他们只是文本文件
6. 不仅每次运行节点要source一下，每打开一个新窗口都要source一下
7.修改文件后一定要重新编译
